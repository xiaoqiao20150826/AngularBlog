=================================


# 한 것.. 
  - 유저, 관리자 페이지.. 수정중. 
  - 에러. 와 메시지. + 리다이렉트.

# 해야할것
  - setupState, setupHandler... 이거 분리좀해야겠다.
  - 관리자..카테고리 관련 마저만들기.
  - 유저 삭제기능. //이건 게시판 마무리 되고 나중에.

# 해야할것 고민   
 - 일단 컨트롤러 전. 리졸브 전. 리다이렉트 하기위해(인증에 대한) 적절한 위치는? 
   ; 리졸브 프로바이더..?같은거?
   => $stateChangstart.( uiRouter, url접근. 으로 변화시 이벤트 발생.)
      ; 여기서 선처리 가능. preventDefault로 기본동작막고.

   => $viewContentLoading(?)   // 시작, 끝 이용하여 프로그레스바.

   => $state..에러처리  // 잘못된 url 로딩?? 같은 것. 

 -  공통적인것 (컨트롤러..및 리졸브, 뷰 에서 접근할 수 있는 것.)
   ;  전역(or 최상위)스코프에 할당된 것. 이면서 앙귤러 서비스

    1) 로그인관련
      ; 사용자 인증 ,  관리자 인증.

    2) 리다이렉트
      ; $state.go, transitionTo 를 이용.

    3) 에러의 처리 기능.
      ; alert한다음에 리다이렉트를 하던가..

  - 트리(앙귤러) 엘리먼트

  - state.  모듈화...하는 것과 관계를 잘생각해보자. 
   ; url에서 다른사람보니 /user/ 이렇게 해놨네. 그러면 마지막 /user와 /user/ 둘다 잡는가?
  ; constant를 이용해서 STATE.MAIN 이런식으로 설정할 수 도 있꾼. //전역적으로 만들자. 쓸모있네.
 ; 

---- 
나중에 시간 여유가 된다면 지연된 로딩... 생각해보자.